import React, { Fragment } from "react";
import RestaurantList from "./RestaurantList";

const MainPage = () => {
  return (
    <Fragment>
      <RestaurantList />
    </Fragment>
  );
};

export default MainPage;
